1.编程程序定义一个长度为10的结构体数组（结构体定义如下），从键盘输入十个学生的信息后输出：

struct student{
    int num;//学号
    char name[20];//姓名
    float score;
} 

（1）十个学生的信息（直接按数组顺序输出即可，无需排序，输出时适当注意数据间的距离和换行）；
（2）最高分学生的信息（默认输入分数都不同）；
（3）最低分学生的信息（默认输入分数都不同）；
（4）10个学生的平均成绩。