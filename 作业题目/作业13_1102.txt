1.（课本第四章课后练习第9题）用结构体类型作为函数的类型，就可以同时返回多个值，请将例 4.3-4 的数组改成结构体类型，修改程序以使其能返回交换后的数据。（提示：main 函数类似如下所示）
int main() 
{
    struct two a={3,5};
    printf("a 的内容是：%d, %d\n", a.a, a.b);
    a=swap(a);
    printf("a 的内容是：%d, %d\n", a.a, a.b);
}
2.在main中输入一个十进制整数，编写函数将其转换为二进制数并输出。
3.在main中输入一个二进制整数，编写函数将其转换为十进制数并输出。
说明：2,3题可以分别做，也可以写一个菜单整合成一个程序